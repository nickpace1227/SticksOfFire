import React from 'react';
import ReactDOM from 'react-dom/client';
import SticksOfFire from './components/Router';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SticksOfFire />
  </React.StrictMode>
);
